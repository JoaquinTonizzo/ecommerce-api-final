<link rel="stylesheet" href="/css/home.css">
<script>
  document.title = 'Home | Ecommerce';
</script>
<main class="home-main">
  <section class="home-hero">
    <h1 class="home-title">¡Bienvenido!</h1>
    <h2 class="home-title" style="font-size:1.3rem; color:#007bff; margin-bottom:8px;">Proyecto Ecommerce - Coderhouse
    </h2>
    <p class="home-desc">Este es un proyecto de backend realizado para el curso de Coderhouse. Explora los
      productos destacados o navega por el catálogo completo.</p>
    <div class="home-btns">
      <a href="/products">Ir al catálogo de productos</a>
      <a href="/realtimeproducts">Ir a administrar productos</a>
    </div>
  </section>
  <div style="max-width:900px;margin:0 auto;">
    <h2 style="margin-bottom:18px;">Productos destacados</h2>
    <ul id="products-list">
      {{#if products.length}}
      {{#each products}}
      <li data-id="{{id}}" class="product-item">
        <div class="product-img-wrapper">
          {{#if thumbnails}}
          <img src="{{thumbnails.[0]}}" alt="Imagen" class="product-img">
          {{else}}
          <div class="product-img no-img" aria-label="Sin Imagen" title="Sin Imagen"></div>
          {{/if}}
        </div>
        <div class="product-info">
          <strong class="product-title">{{title}}</strong>
          <span class="product-desc"><b>Descripción:</b> {{description}}</span>
          <span class="product-code"><b>Código:</b> {{code}}</span>
          <span class="product-price"><b>Precio:</b> ${{price}}</span>
          <span class="product-stock"><b>Stock:</b> {{stock}}</span>
          <span class="product-category"><b>Categoría:</b> {{category}}</span>
          <span class="product-status"><b>Status:</b> {{#if status}}Activo{{else}}Inactivo{{/if}}</span>
        </div>
      </li>
      {{/each}}
      {{else}}
      <li style="color:#ffd600; text-align:left; font-size:1.15em; margin:32px; list-style:none;">No hay productos.</li>
      {{/if}}
    </ul>
  </div>
</main>